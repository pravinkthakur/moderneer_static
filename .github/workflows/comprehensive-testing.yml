name: 🧪 Tests (Disabled)

# Tests are currently disabled in CI/CD
# Run tests locally using:
#   npm run test        - Run skim pack (fast tests)
#   npm run test:full   - Run full test suite
#   npm run test:smoke  - Run smoke tests only

on:
  workflow_dispatch:

jobs:
  info:
    name: ℹ️ Tests Disabled
    runs-on: ubuntu-latest
    steps:
      - name: ℹ️ Information
        run: |
          echo "🚫 Automated tests are currently disabled in CI/CD"
          echo ""
          echo "✅ Run tests locally:"
          echo "   npm run test        - Skim pack (fast)"
          echo "   npm run test:full   - Full suite"
          echo "   npm run test:smoke  - Smoke tests only"
