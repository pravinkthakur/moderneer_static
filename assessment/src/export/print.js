import { buildOutline } from './outline.js'; export function openPrint(results){const r=results||(window.compute&&window.compute(false))||{};const outline=buildOutline(r);const json=JSON.stringify(outline);const b64=btoa(unescape(encodeURIComponent(json))).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'');const url=new URL('./print_view.html#'+b64, import.meta.url);window.open(url,'_blank','noopener');}